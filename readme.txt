The essence of the project is an application that is used by the residents of the city. The project is linked to a database, so you can request specific information.
The database stores information about users, trainschedule, buildings, labor searcher, currency.
Implemented classes with authorization, registration, the main class connects the bd and starts the application.